# Để chạy thực nghiệm mô hình, chạy câu lệnh:
python main.py --model MGRec

# Mặc định mô hình sẽ chạy trên tập dữ liệu Music, để thay đổi tập dữ liệu, truy cập vào ./config/modelconf/mgrec.yaml, thay đổi giá trị của biến name trong mục data thành tên tập dữ liệu tương ứng, ví dụ ml-20m.
